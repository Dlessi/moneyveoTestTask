import {StyleSheet} from 'react-native';

const mainBgColor = '#eeeeee';

const styles = StyleSheet.create({
  wrapper: {
    height: 70,
    width: '90%',
    backgroundColor: mainBgColor,
    position: 'absolute',
    bottom: 0,
  },
  input: {
    height: '100%',
    width: '100%',
  },
});

export default styles;
